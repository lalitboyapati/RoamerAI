# Browser-visible. The key here MUST be the search-only key printed by
# `python index_typesense.py setup` — never the admin key.
VITE_TYPESENSE_HOST=localhost
VITE_TYPESENSE_PORT=8108
VITE_TYPESENSE_PROTOCOL=http
VITE_TYPESENSE_SEARCH_KEY=
